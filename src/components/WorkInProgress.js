const WorkInProgress = (props) => {
    return (
        <div className={` ${props.className} p-8 bg-orange-300 w-full`}>
            This page is very much a work in progess!  So no judgements!
        </div>
    )
}

export default WorkInProgress;